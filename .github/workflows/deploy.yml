name: Deploy WordPress theme via SFTP

on:
  push:
    branches:
      - codex/-wordpress
  workflow_dispatch:

concurrency:
  group: wordpress-theme-production
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  deploy:
    name: Upload theme
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'push' && github.ref == 'refs/heads/codex/-wordpress')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install SFTP client
        run: sudo apt-get update && sudo apt-get install --yes lftp

      - name: Validate deployment settings
        env:
          FTP: ${{ secrets.FTP }}
          PORT: ${{ secrets.PORT }}
          NAME: ${{ secrets.NAME }}
          PASS: ${{ secrets.PASS }}
          URL: ${{ secrets.URL }}
        run: |
          set -euo pipefail
          test -n "$FTP"
          test -n "$PORT"
          test -n "$NAME"
          test -n "$PASS"
          test -n "$URL"
          [[ "$PORT" =~ ^[0-9]+$ ]]
          [[ "$URL" =~ ^/[A-Za-z0-9._/-]+/$ ]]

      - name: Deploy current repository via SFTP
        env:
          FTP: ${{ secrets.FTP }}
          PORT: ${{ secrets.PORT }}
          NAME: ${{ secrets.NAME }}
          PASS: ${{ secrets.PASS }}
          URL: ${{ secrets.URL }}
        run: |
          set -euo pipefail
          lftp \
            -u "$NAME","$PASS" \
            -p "$PORT" \
            "sftp://$FTP" \
            -e "
              set cmd:fail-exit yes;
              set net:max-retries 2;
              set net:timeout 20;
              # GitHub runners have an empty known_hosts file; lftp records the
              # server key for this ephemeral deployment session.
              set sftp:auto-confirm yes;
              mirror --reverse --verbose --parallel=4 \
                --exclude-glob .git/ \
                --exclude-glob .github/ \
                --exclude-glob .env \
                --exclude-glob node_modules/ \
                --exclude-glob vendor/ \
                ./ $URL;
              bye
            "
